# local_manifest
Local manifest to build ROMs for Samsung Galaxy A22 4G
primarily for LineageOS but could work for other ROMs (currently tested other ROM other than Lineage is PixelOS)

- Put `local_manifest.xml` into `.repo/local_manifests/` (create one if it doesnt exist)
- Then run `repo sync`
